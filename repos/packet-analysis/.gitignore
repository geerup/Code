# Secrets — never commit (baseline portfolio patterns)
.env
.env.*
!.env.example
*.key
*.pem
*_rsa
*_ed25519
id_*
*.wallet
wallet.dat
*.keys
bitcoin.conf
monerod.conf
secrets/

# Capture data — only small, sanitized, intentionally-committed pcaps belong here.
# Ignore everything by default; force-add reviewed samples with `git add -f`.
*.pcap
*.pcapng
*.cap

# OS / editor cruft
.DS_Store
*.swp
*~
