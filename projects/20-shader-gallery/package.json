{
  "name": "shader-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WebGL fragment-shader gallery (full-screen quad, zero deps).",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8082"
  }
}
